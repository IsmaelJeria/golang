package main

/*
import "fmt"

type tasklist struct {
	tasks []*task
}

func (t *tasklist) addTaskList(ts *task) {
	t.tasks = append(t.tasks, ts)
}

func (t *tasklist) delTaskList(index int) {
	t.tasks = append(t.tasks[:index], t.tasks[index+1:]...)
}

type task struct {
	name        string
	description string
	completed   bool
}

func (t *task) flagCompleted() {
	t.completed = true
}

func main() {
	h := task{
		name:        "do go course",
		description: "doing go course",
	}

	h.flagCompleted()
	fmt.Printf("%+v\n", h)

	tl := tasklist{}
	tl.addTaskList(&h)

	fmt.Printf("%+v\n", tl)
	fmt.Println(len(tl.tasks))

}


*/
